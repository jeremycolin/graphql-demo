# GraphQL Demo Repository

### presentation contains a Spectacle demo

`cd presentation && yarn start`

### server contains a GraphQL Server

`cd server && yarn start`

### front contains a GraphQL Client

`cd front && yarn start`
